{
  "name": "clauderouter",
  "version": "0.2.0",
  "description": "Automatic cost optimization and routing layer for the Anthropic Claude API",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "claude-router": "dist/cli/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc --watch",
    "test": "node --require ts-node/register --test tests/pricing.test.ts tests/classifier.test.ts tests/escalation.test.ts tests/logger.test.ts tests/router.test.ts",
    "test:build": "npm run build && node --test 'dist/tests/**/*.test.js'",
    "lint": "tsc --noEmit",
    "prepublishOnly": "npm run build && npm run test:build"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.52.0",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "anthropic",
    "claude",
    "routing",
    "cost-optimization",
    "llm"
  ],
  "license": "MIT"
}
